<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="story">
	<select id="selectAll" resultType="com.careme.model.dto.StoryBoardDto">
		select * from story_board
	</select>
<!-- 	<select id="list" resultType="com.careme.model.dto.StoryBoardDto">
		select title, profile_image_file_name, from story_board natural join members
		where 
	</select>
	
	<insert id="articleInsert" parameterType="hashmap">
		insert into story_board 
		value(#{story_board_idx}, #{member_idx}, #{tag_idx}, 
		#{title}, #{content}, #{reg_date}, #{update_date})
	</insert>
	
	<insert id="fileInsert" parameterType="hashmap">
		insert into story_file
		values(#{story_board_idx}, #{file_name}, #{file_path}, 
		#{file_size}, #{reg_date}, #{update_date})
	</insert> -->
</mapper>