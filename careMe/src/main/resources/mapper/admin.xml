<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="admin">
	<select id="selectMemberList" resultType="com.careme.model.dto.MemberDto">
		select * from members where del_yn = 'n' limit 20
	</select>
	
	<select id="searchMemberList" resultType="com.careme.model.dto.MemberDto" parameterType="HashMap">
		select SQL_CALC_FOUND_ROWS * 
		from members 
		where del_yn = 'n' 
		and ${searchColumn} like concat('%', #{searchText}, '%')
		limit ${pageStart}, ${limit}
	</select>
	
	<select id="selectTotalCount" resultType="int">
		SELECT FOUND_ROWS()
	</select>
	
	<update id="deleteMember">
		update members set del_yn = 'y' where member_idx = #{member_idx}
	</update>

</mapper>